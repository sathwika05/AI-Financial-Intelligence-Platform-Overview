# frontend/src/lib

**Shared helpers.** Formatting, routing and the small pure functions the screens share.

| | |
|---|---|
| `format.ts / labels.ts` | number, currency and label formatting |
| `citations.ts / companies.ts / entries.ts` | shaping API responses for display |
| `router.ts` | the hash router |
| `consoleSession.ts` | query history for the session |

_Implementations are private. This file lists what lives here and what it is responsible for._

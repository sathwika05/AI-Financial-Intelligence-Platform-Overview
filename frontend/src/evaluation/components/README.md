# frontend/src/evaluation/components

**Dashboard components.** The shared furniture the views compose.

| | |
|---|---|
| `RunBar.tsx` | provider, question set and the retrieval flags a run is launched with |
| `Sidebar.tsx` | navigation |
| `charts.tsx / primitives.tsx` | the chart and tile vocabulary |
| `ClaimTiles.tsx` | claim-audit results |
| `RetrievalPipelineDiagram.tsx` | the retrieval stages a run actually used |

_Implementations are private. This file lists what lives here and what it is responsible for._

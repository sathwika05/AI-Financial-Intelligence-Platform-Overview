# frontend/src/admin

**Admin screens.** Provider configuration, indexing, ingestion and the human review queue.

| | |
|---|---|
| `ProvidersScreen.tsx` | providers, tiers and keys |
| `IngestionScreen.tsx` | upload, collection and the event log |
| `HumanReviewScreen.tsx` | the escalation queue |
| `SecurityScreen.tsx` | the security event feed |

_Implementations are private. This file lists what lives here and what it is responsible for._

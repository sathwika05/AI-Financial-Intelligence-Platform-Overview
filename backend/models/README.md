# backend/models

**SQLAlchemy models.** 23 tables across corpus, configuration, evaluation and operations. The chunk table carries a 1536-dimension vector column.

| | |
|---|---|
| `db_models.py` | every table, in one module |

_Implementations are private. This file lists what lives here and what it is responsible for._

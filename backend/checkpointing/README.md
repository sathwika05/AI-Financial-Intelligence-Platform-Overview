# backend/checkpointing

**Checkpointing.** Graphs compile with a checkpointer so a run's state stays addressable by thread id after it finishes.

| | |
|---|---|
| `saver.py` | checkpointer construction |

_Implementations are private. This file lists what lives here and what it is responsible for._

# alembic/versions

**Revisions.** 23 of them. The first *alters* tables it expects `create_all` to have made, which is why this chain cannot build a database from scratch.

| | |
|---|---|
| `(revisions)` | each named for the change it makes |

_Implementations are private. This file lists what lives here and what it is responsible for._

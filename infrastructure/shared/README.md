# infrastructure/shared

**Shared resources.** What both environments draw on, kept in its own state.

| | |
|---|---|
| `registry.tf` | the container registry |
| `certificate.tf` | the TLS certificate |

_Implementations are private. This file lists what lives here and what it is responsible for._
